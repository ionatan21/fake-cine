---
import Layout from "@/layouts/Layout.astro";
import "@/styles/globals.css";
import ReactNavbar from "@/components/navbar/Navbar";
import Footer from "@/components/home/footer.astro";
---

<Layout>
  <ReactNavbar client:visible />
  <main
    class="min-h-screen mt-8 md:mt-4 bg-gray-900 w-screen flex flex-col items-center justify-center px-4"
  >
    <section
      class="grid gap-6 p-2 md:grid-cols-2 sm:grid-cols-1 w-full max-w-7xl"
    >
      <div>
        <h1 class="text-4xl font-bold text-center text-[#f60]">Contáctanos</h1>
        <p class="text-center text-gray-300">
          ¿Tienes dudas o sugerencias? ¡Escríbenos!
        </p>

        <form
          onsubmit="event.preventDefault(); document.getElementById('sentMsg').classList.remove('hidden')"
          class="space-y-4 p-2"
        >
          <div>
            <label for="nombre" class="block mb-1">Nombre</label>
            <input
              type="text"
              id="nombre"
              name="nombre"
              required
              class="w-full p-3 border-[1px] border-[#f60] rounded-lg bg-gray-800 text-white focus:outline-none focus:ring-2 focus:ring-[#f60]"
            />
          </div>

          <section
            class="flex flex-col md:flex-row relative gap-2 justify-center items-center"
          >
            <div class="w-full">
              <label for="correo" class="mb-1 w-full">Correo electrónico</label>
              <input
                type="email"
                id="correo"
                name="correo"
                required
                class="w-full border-[1px] border-[#f60] p-3 rounded-lg bg-gray-800 text-white focus:outline-none focus:ring-2 focus:ring-[#f60]"
              />
            </div>

            <div class="w-full">
              <label for="telefono" class="mb-1 w-full">Teléfono</label>
              <input
                type="tel"
                id="telefono"
                name="telefono"
                required
                class="w-full p-3 border-[1px] focus:ring-[#f60] border-[#f60] rounded-lg bg-gray-800 text-white focus:outline-none focus:ring-2"
              />
            </div>
          </section>

          <div>
            <label for="mensaje" class="block mb-1">Mensaje</label>
            <textarea
              id="mensaje"
              name="mensaje"
              rows="4"
              required
              class="w-full p-3 border-[1px] focus:ring-[#f60] border-[#f60] rounded-lg bg-gray-800 text-white focus:outline-none focus:ring-1"
            ></textarea>
          </div>

          <button
            type="submit"
            class="w-full bg-[#2270b2] opacity-90 hover:opacity-100 text-white border-2 border-white font-semibold py-3 px-6 rounded-lg transition duration-300"
          >
            Enviar mensaje
          </button>

          <p
            id="sentMsg"
            class="hidden mt-4 text-green-400 text-center font-semibold"
          >
            ¡Tu mensaje ha sido enviado! 💌
          </p>
        </form>
      </div>
      <div class="flex flex-col items-center justify-center p-6 rounded-lg">
        <img
          src="/logo-fake.webp"
          alt="logo FakeCine"
          class="w-40 md:w-96 mb-4"
        />
      </div>
    </section>
  </main>
  <Footer />
</Layout>
